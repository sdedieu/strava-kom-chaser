import { Component } from '@angular/core';

@Component({
    selector: 'app-auth-redirect',
    standalone: true,
    template: ``,
})
export class AuthRedirectComponent {
    constructor() { console.log('auth comp') }
}